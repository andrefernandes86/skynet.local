services:
  openwebui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "3000:8080"
    volumes:
      - open-webui:/app/backend/data
    security_opt:
      - no-new-privileges:true

volumes:
  open-webui:
